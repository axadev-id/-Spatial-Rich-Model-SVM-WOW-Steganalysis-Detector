# GPU-Accelerated Steganalysis with SRM Features
# Core Python libraries
numpy>=1.21.0
scipy>=1.7.0
scikit-learn>=1.1.0
matplotlib>=3.5.0
seaborn>=0.11.0
pandas>=1.3.0
tqdm>=4.62.0
joblib>=1.1.0

# Computer Vision and Image Processing
opencv-python>=4.5.0
Pillow>=8.3.0

# GPU Acceleration (optional but recommended)
# Install RAPIDS cuML for GPU acceleration:
# conda install -c rapidsai -c nvidia -c conda-forge cuml=23.10 python=3.10 cudatoolkit=11.8
# or pip install cuml-cu11 --extra-index-url=https://pypi.nvidia.com
cuml>=23.10.0; sys_platform != "win32"  # Linux/Mac only initially
cupy-cuda11x>=11.0.0; sys_platform != "win32"  # Linux/Mac only initially

# For Windows users, install manually:
# pip install cupy-cuda11x (requires CUDA 11.x)
# Follow RAPIDS cuML installation guide for Windows

# High-performance computing
numba>=0.56.0
multiprocessing-logging>=0.3.0

# Configuration and serialization
PyYAML>=6.0
configparser>=5.2.0

# System monitoring
psutil>=5.8.0
GPUtil>=1.4.0

# Progress bars and logging
colorama>=0.4.4
rich>=12.0.0

# Jupyter notebook support (optional)
jupyter>=1.0.0
ipykernel>=6.0.0
notebook>=6.4.0

# Additional ML libraries
xgboost>=1.6.0  # Alternative classifier
imbalanced-learn>=0.9.0  # For handling imbalanced datasets

# Visualization enhancements
plotly>=5.0.0
bokeh>=2.4.0

# File format support
h5py>=3.6.0  # HDF5 support for large datasets
tables>=3.7.0  # PyTables for efficient data storage

# Development and testing (optional)
pytest>=7.0.0
pytest-cov>=3.0.0
black>=22.0.0
flake8>=4.0.0
mypy>=0.950